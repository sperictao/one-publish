[package]
name = "publish-planner"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
publish-adapters = { path = "../publish-adapters" }
publish-domain = { path = "../publish-domain" }

[dev-dependencies]
serde_json.workspace = true
