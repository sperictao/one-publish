name: Temporary Composition Registry Boundary

on:
  push:
    branches: [tmp/fix-composition-registry-boundary]

permissions:
  contents: write

jobs:
  patch-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Apply focused correction
        run: python .github/scripts/tmp-fix-composition-registry-boundary.py
      - name: Diff check
        run: git diff --check
      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
      - name: Clippy
        run: cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings
      - name: Regression test
        run: cargo test --manifest-path src-tauri/Cargo.toml automation::tests::bindings_keep_their_revision_until_an_explicit_upgrade_is_applied
      - name: Config composition tests
        run: cargo test --manifest-path src-tauri/Cargo.toml config_export::tests
      - name: Full Rust tests
        run: cargo test --manifest-path src-tauri/Cargo.toml
      - name: Commit clean target tree
        run: |
          git rm .github/scripts/tmp-fix-composition-registry-boundary.py .github/workflows/tmp-composition-registry-boundary.yml
          git diff --check
          git config user.name "Eric Tao"
          git config user.email "13306434+sperictao@users.noreply.github.com"
          git add src-tauri/src/config_export.rs src-tauri/src/publish_runtime.rs src-tauri/src/publish_runtime/source.rs src-tauri/src/store/types.rs
          git commit -m "fix(runtime): defer adapter compatibility to registry"
          git push origin HEAD:tmp/fix-composition-registry-boundary
