name: Quality

on:
  push:
    branches:
      - ci-clippy-fix-20260910-v2

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate branch workflow
        run: echo workflow-ok
