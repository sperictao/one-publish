# TASK-0602: Provider-Specific Config Mapping in Command Import

Status: done
Owner: OpenClaw
Links:
- PR: (tbd)

## Context

Command import can parse multiple providers, but only dotnet parameters are mapped into current publish form state.

## Goals

- Provide provider-specific mapping from parsed spec to editable UI state
- Avoid silent no-op for non-dotnet import

## Non-Goals

- Execute provider commands
- Redesign parameter editor architecture

## Delivered

- Added `mapImportedSpecByProvider()` to unify import mapping and mapped/unmapped field classification
- Added schema-aware non-dotnet parameter state in App workflow (`cargo/go/java`)
- Wired non-dotnet provider card to `ParameterEditor` so imported values are directly editable
- Added import feedback card showing mapped/unmapped fields for the active provider
- Reused mapped non-dotnet parameters in Config dialog save path

## Acceptance Criteria

- [x] AC1: cargo/go/java import can produce editable provider-specific state
- [x] AC2: Import feedback clearly shows mapped/unmapped fields
