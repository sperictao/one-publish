# TASK-0204: Java Provider

Status: doing
Owner: OpenClaw
Links:
- PR: (tbd)

## Context

Go provider exists (TASK-0203). Next we add a Java provider to compile a spec into an execution plan.

## Goals

- Add provider id `java` with a manifest.
- Compile a minimal java spec into one build step.

## Non-Goals

- Full Maven/Gradle auto-detection.
- Full packaging/signing.

## Requirements

- R1: `provider_id = "java"` resolves.
- R2: Compiling a java spec produces a single step (pick one: `gradle.build` or `maven.package`).

## Technical Design

Best initial slice: support Gradle wrapper execution (common, repo-local):
- step id: `gradle.build`
- title: `./gradlew build`

## TDD Plan

- [ ] Registry resolves `java`.
- [ ] Compiler produces `gradle.build` for java specs.

## Acceptance Criteria

- [ ] AC1: `cargo test --manifest-path src-tauri/Cargo.toml` passes.
