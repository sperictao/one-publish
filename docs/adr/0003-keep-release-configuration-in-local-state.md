# 发布配置仅保存在 One Publish 本地状态

目标仓库的 Tauri 发布配置由 One Publish 本地状态独占保存，仓库不提交额外的 One Publish 配置文件，生成后的 workflow 也不作为反向恢复配置的来源。为了支持换机和灾备，One Publish 提供显式导出与导入，但备份文件不包含令牌、私钥或密码；因此其他设备必须导入备份或重新配置后才能继续管理该仓库。
