# 发布配置仅保存在 One Publish 本地状态

目标仓库的 Tauri 发布配置作为普通 `ConfigProfile` 由 One Publish 现有仓库配置状态独占保存，不建立 Tauri 专用配置存储；发布尝试作为运行记录单独持久化。仓库不提交额外的 One Publish 配置文件，生成后的 workflow 也不作为反向恢复配置的来源；通用配置导出不包含身份、当前选择、令牌、私钥或密码，因此其他设备必须导入或重新配置后才能继续管理该仓库。
