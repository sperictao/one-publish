# Pin automation runtime revisions

Installed automation pins the One Publish Runner version and digest together with the Publish Plan contract and adapter versions. Application upgrades only report an available runtime revision, and bindings continue reproducibly until an explicit reviewed update replaces the projection; floating latest tags and unverified installer scripts are prohibited.
