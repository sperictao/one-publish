# 发布取消以原子远端提交为不可逆边界

最终确认前，取消发布会恢复 One Publish 本次生成的版本与 release notes 变更；若目标文件在此期间被再次修改，则停止恢复并报告冲突，绝不覆盖用户内容。最终确认使用原子 push 同时提交默认分支和版本标签，避免只成功一半；标签建立后，取消只能请求停止 GitHub workflow 或本地监控，并把发布尝试记为已取消，不能删除或移动远端版本身份。
