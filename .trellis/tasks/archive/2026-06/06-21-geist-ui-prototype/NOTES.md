# Prototype Decision

## 2026-06-21

Decision: continue with the normal app prototype direction.

Interpretation:

- Use `A - Dense Workbench` as the baseline direction because it preserves the current three-column One Publish workbench.
- Do not pursue `B - Inspector Focus` or `C - Command Center` as the main production direction for now.
- Later production child tasks should absorb the Geist refinements into the normal app path rather than replacing the app with a new layout model.

Recommended next child task:

- `geist-workbench-normal-absorption`

Initial absorption scope:

- Remove prototype-only switching when the direction is folded into production.
- Keep the default `/` route as the normal app.
- Apply Geist refinements incrementally to the existing app shell, repository panel, publish config panel, and publish run area.
